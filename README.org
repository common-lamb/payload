* payload

utility to gather hash compress and send large files over the network

* workflow

#+begin_src lisp
  (asdf:load-system :metarepl.file-processing.payload)
  (use-package :payload)

  ;; parameters are tested before flight and will prompt when wrong

  ;; *project-name*
  ;; *remote-login*
  ;; *target-dir*
  ;; *payload-files*

  (setf *project-name* "data-transfer-0")
  (setf *remote-login* "user@computer.com:")
  (setf *target-dir* "~/path/to/target/")
  (setf *payload-files* '("/path/to/file0.type"
                          "/path/to/file1.type"))


  ;; functions
  create-transfer-directories
  create-documents
  test-transfer
  check-payload-files
  link-all
  hash-all
  compress
  transfer
  cleanup


  ;; all in in one with tests
  deliver-payload
#+end_src
